{%- macro small(params)-%}
<{{params.tagName | default('small')}} {%- if params.className %} class="{{params.className}}"{% endif %}>The quick brown fox jumped over the lazy dog.</{{params.tagName | default('small')}}>
{%- endmacro -%}

{{small({
    className: className,
    tagName: tagName
})}}
